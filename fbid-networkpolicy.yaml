apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: fbid
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/fbid: "true"
  podSelector:
    matchLabels:
      io.kompose.network/fbid: "true"
